# 自定义资产/负债类型功能测试计划

## 测试概述

本文档用于验证自定义资产/负债类型功能的完整实现，包括：
- 数据库迁移验证
- 自定义类型 CRUD 操作
- UI 交互测试
- 资产筛选功能
- 删除保护机制
- 历史数据保留

---

## 测试前准备

### 1. 环境检查

```bash
# 检查 Rust 编译
cd rust_core
cargo build --release

# 检查 Flutter 依赖
cd ../flutter_app
flutter pub get
flutter analyze
```

### 2. 清理旧数据（可选）

```bash
# 删除现有数据库以便测试迁移
# Windows: %APPDATA%\localfamily-asset\
# 删除 assets.db 文件
```

### 3. 启动应用

```bash
cd flutter_app
flutter run -d windows
```

---

## Phase 1: 数据库迁移测试

### 测试目标
验证 v4 迁移成功创建 `custom_asset_types` 表

### 测试步骤

1. **首次启动检查**
   - 启动应用，完成初始设置（密码/助记词）
   - 使用 SQLite 工具打开 `assets.db`
   - 验证表结构：
   ```sql
   .schema custom_asset_types
   ```

2. **预期结果**
   ```sql
   CREATE TABLE [IF NOT EXISTS] custom_asset_types (
       id TEXT PRIMARY KEY,
       name TEXT NOT NULL,
       icon_name TEXT NOT NULL,
       is_liability INTEGER NOT NULL DEFAULT 0,
       created_at INTEGER NOT NULL
   );
   ```

3. **验证索引**
   ```sql
   .schema idx_custom_types_is_liability
   ```
   应该存在 `is_liability` 字段的索引

### 自动化测试脚本（可选）

创建 `rust_core/tests/test_migration.rs`：

```rust
#[test]
fn test_custom_types_table_exists() {
    use rusqlite::Connection;

    let conn = Connection::open_in_memory().unwrap();
    super::migrate_v4_custom_types(&conn).unwrap();

    // 检查表是否存在
    let table_exists: bool = conn
        .query_row(
            "SELECT COUNT(*) FROM sqlite_master WHERE type='table' AND name='custom_asset_types'",
            [],
            |row| row.get(0),
        )
        .unwrap();

    assert!(table_exists, "custom_asset_types table should exist");
}
```

---

## Phase 2: 自定义类型 CRUD 测试

### 测试 2.1: 创建自定义资产类型

**步骤：**
1. 启动应用，进入"资产"标签页
2. 点击筛选栏右侧的"+"按钮
3. 在弹出的管理对话框中点击右下角"+"按钮
4. 输入类型名称："理财产品"
5. 选择图标："星星"
6. 点击"创建"按钮

**预期结果：**
- 显示 SnackBar："创建成功"
- 对话框关闭
- "理财产品"出现在筛选栏中
- "理财产品"显示在管理对话框列表中

**验证数据：**
```sql
SELECT * FROM custom_asset_types;
```
应该看到新创建的记录，id 格式为 `custom_<uuid>`

### 测试 2.2: 创建自定义负债类型

**步骤：**
1. 切换到"负债"标签页
2. 重复上述步骤
3. 输入类型名称："其他借款"
4. 选择图标："收藏"
5. 点击"创建"

**预期结果：**
- 负债筛选栏显示"其他借款"
- 资产筛选栏不显示该类型

**验证数据：**
```sql
SELECT * FROM custom_asset_types WHERE is_liability = 1;
```

### 测试 2.3: 查看自定义类型列表

**步骤：**
1. 点击筛选栏"+"按钮
2. 查看管理对话框

**预期结果：**
- 显示所有已创建的自定义类型
- 每个类型显示正确的图标和名称
- 按创建时间倒序排列

### 测试 2.4: 编辑功能验证（当前不支持）

**步骤：** 尝试编辑现有类型

**预期结果：** 无编辑入口（按设计要求）

---

## Phase 3: 资产创建测试（使用自定义类型）

### 测试 3.1: 创建使用自定义类型的资产

**步骤：**
1. 确保已创建"理财产品"类型
2. 点击右下角"+"按钮添加资产
3. 填写表单：
   - 资产名称："招商银行理财产品"
   - 选择类型："理财产品"
   - 金额：100000
   - 其他字段根据需要填写
4. 点击"添加资产"

**预期结果：**
- 资产成功添加
- 资产列表中显示该资产
- 资产类型显示为"理财产品"
- 统计卡片正确计算总额

**验证数据：**
```sql
SELECT id, type, name, amount FROM assets WHERE type LIKE 'custom_%';
```

### 测试 3.2: 使用自定义类型筛选资产

**步骤：**
1. 在资产筛选栏点击"理财产品"
2. 查看资产列表

**预期结果：**
- 只显示类型为"理财产品"的资产
- 列表标题显示："资产列表 (1)" 或相应的数量

### 测试 3.3: 清除筛选

**步骤：** 点击"全部资产"

**预期结果：**
- 显示所有资产
- 数量恢复为总数

---

## Phase 4: 删除保护测试

### 测试 4.1: 删除正在使用的类型（应失败）

**步骤：**
1. 确保已创建使用"理财产品"的资产
2. 打开管理对话框
3. 点击"理财产品"的删除按钮
4. 确认删除

**预期结果：**
- 显示 SnackBar："该类型正在使用中，无法删除"
- 类型未被删除
- 管理对话框仍显示该类型

**验证数据：**
```sql
-- 检查类型是否仍存在
SELECT * FROM custom_asset_types WHERE name = '理财产品';
-- 应该返回该记录
```

### 测试 4.2: 删除未使用的类型（应成功）

**步骤：**
1. 创建一个新的自定义类型（不创建任何资产）
2. 打开管理对话框
3. 点击该类型的删除按钮
4. 确认删除

**预期结果：**
- 显示 SnackBar："删除成功"
- 管理对话框列表更新
- 筛选栏中移除该类型

---

## Phase 5: 历史数据保留测试

### 测试 5.1: 删除类型后的历史记录

**步骤：**
1. 创建一个测试资产，使用自定义类型
2. 查看资产详情/审计日志
3. 删除自定义类型（先删除所有资产）
4. 查看审计日志中的历史记录

**预期结果：**
- 历史记录中仍显示原始类型名称
- 数据不会丢失
- UI 显示：原始名称而不是引用已删除的 ID

**验证数据：**
```sql
-- 资产的 type 字段应保留原始值
SELECT type FROM assets WHERE id = 'test_asset_id';
-- 应返回：custom_xxx
```

---

## Phase 6: 边界条件测试

### 测试 6.1: 创建重复名称的类型

**步骤：** 尝试创建同名类型

**预期结果：**
- 当前允许同名（数据库无唯一约束）
- 两个类型使用不同的 ID

### 测试 6.2: 空名称验证

**步骤：** 创建时不输入名称

**预期结果：**
- 显示验证错误："请输入名称"
- 无法提交

### 测试 6.3: 特殊字符名称

**步骤：** 输入特殊字符，如 "理财<A>&B"产品"

**预期结果：**
- 应该正常存储（使用参数化查询）
- 显示正确

### 测试 6.4: 长名称

**步骤：** 输入超长名称（> 100 字符）

**预期结果：**
- 数据库无长度限制（TEXT 类型）
- UI 可能截断显示

---

## Phase 7: 性能测试

### 测试 7.1: 大量自定义类型

**步骤：**
1. 创建 50+ 个自定义类型
2. 查看筛选栏滚动性能
3. 测试筛选响应速度

**预期结果：**
- 筛选栏滚动流畅
- 筛选操作响应 < 100ms

### 测试 7.2: 大量资产筛选

**步骤：**
1. 创建 100+ 个资产
2. 使用自定义类型筛选

**预期结果：**
- 筛选响应 < 200ms
- UI 不卡顿

---

## 测试检查清单

### 数据库层
- [ ] v4 迁移成功执行
- [ ] custom_asset_types 表结构正确
- [ ] 索引创建成功
- [ ] CRUD 操作正常
- [ ] is_in_use 查询准确

### FFI 接口层
- [ ] create_custom_asset_type 返回正确 ID
- [ ] get_custom_asset_types 返回完整列表
- [ ] delete_custom_asset_type 正确删除
- [ ] is_custom_type_in_use 返回准确结果

### Dart 状态管理
- [ ] CustomTypeProvider 正确加载数据
- [ ] assetCustomTypes 和 liabilityCustomTypes 正确过滤
- [ ] 创建后自动刷新列表
- [ ] 删除后自动刷新列表

### UI 组件
- [ ] 筛选栏正确显示混合类型
- [ ] 内置类型和自定义类型样式一致
- [ ] 管理对话框正确显示列表
- [ ] 创建对话框验证正确
- [ ] 图标选择交互正常
- [ ] 删除确认对话框显示

### 资产表单
- [ ] 类型选择显示自定义类型
- [ ] 保存时使用正确的类型 ID
- [ ] 编辑资产时类型显示正确

### 错误处理
- [ ] 网络错误时显示提示
- [ ] 创建失败时显示提示
- [ ] 删除保护提示正确

---

## 已知问题和限制

1. **编辑功能**：当前不支持编辑自定义类型（符合设计要求）
2. **名称重复**：允许创建同名类型
3. **类型数量**：UI 在 100+ 类型时可能需要优化

---

## 测试报告模板

```markdown
# 测试执行报告

**测试日期**：YYYY-MM-DD
**测试人员**：[姓名]
**应用版本**：[版本号]

## 测试结果摘要

| 模块 | 通过 | 失败 | 阻塞 |
|------|------|------|------|
| 数据库迁移 | ✅ | - | - |
| CRUD 操作 | ✅ | - | - |
| UI 交互 | ✅ | - | - |
| 资产筛选 | ✅ | - | - |
| 删除保护 | ✅ | - | - |

## 详细测试结果

[填写每个测试用例的详细结果]

## 发现的问题

1. [问题描述]
   - 重现步骤：
   - 预期结果：
   - 实际结果：
   - 严重程度：

## 建议

[改进建议]
```

---

## 回归测试清单

修改代码后需要执行的回归测试：

- [ ] 启动应用无崩溃
- [ ] 现有内置类型资产正常显示
- [ ] 添加内置类型资产正常
- [ ] 添加自定义类型资产正常
- [ ] 筛选功能正常
- [ ] 统计数据正确
- [ ] 审计日志正常
